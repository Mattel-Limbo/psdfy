name: Create Release (DEPRECATED)

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Workflow Deprecated
      run: |
        echo "This workflow is deprecated and has been replaced by:"
        echo "- sync-tag-to-master-and-publish.yml"
        echo ""
        echo "The new workflow automatically:"
        echo "1. Updates version in pyproject.toml and psdfy/__init__.py"
        echo "2. Commits and pushes to master"
        echo "3. Publishes to PyPI"
        echo "4. Creates a GitHub Release"
        exit 0
