# This workflow has been replaced by:
# - sync-master-to-latest-tag.yml: Syncs master to latest tag
# - sync-tag-to-master-and-publish.yml: Syncs new tags to master and publishes to PyPI
#
# To use the new workflow:
# 1. Push changes to master - this will update the latest tag (v1.0.5)
# 2. Create a new tag: git tag -a v1.0.6 -m "Update latest tag to v1.0.6"
# 3. Push the tag: git push origin v1.0.6
# 4. The new tag will automatically sync to master and publish to PyPI

name: Publish to PyPI on Push (DEPRECATED)

on:
  push:
    branches:
      - master
    paths:
      - 'pyproject.toml'
      - 'psdfy/**'
      - '.github/workflows/publish-on-push.yml'

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    
    steps:
    - name: Workflow Deprecated
      run: |
        echo "This workflow is deprecated and has been replaced by:"
        echo "- sync-master-to-latest-tag.yml"
        echo "- sync-tag-to-master-and-publish.yml"
        exit 0
