name: Publish to PyPI (DEPRECATED)

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    
    steps:
    - name: Workflow Deprecated
      run: |
        echo "This workflow is deprecated and has been replaced by:"
        echo "- sync-tag-to-master-and-publish.yml"
        echo ""
        echo "Publishing to PyPI is now handled automatically when you push a new tag."
        exit 0
